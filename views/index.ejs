<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Node.js Crash Course</title>
</head>
<body>
    <% const name = 'mario' %>
  <h1> hello your <%= title %></h1>
  <h2>Your path to becoming a Node.js ninja!</h2>
  <%= name %>
  <nav>
      <a href = "/">Home page</a>
      <a href = "/about">About page</a>
  </nav>
  <div>
    <h2></h2>
    <% if (blogs.length > 0) { %>
        <% blogs.forEach(blog => { %>
            <h3><%= blog.title %></h3>
            <p><%= blog.snippet %></p>
            <% })%>

        <% }%>
</div>
</body>
</html>